# TV go iOS 2.4.11

本轮修复聚焦 mpv 内核的崩溃风险、起播超时的失效场景，以及若干并发与状态管理问题。

## 修复

- **mpv 核心退出时的 use-after-free（关键）**
  - mpv 收到致命退出（SHUTDOWN）事件时，销毁句柄被派发到全局并发队列，可能与后台采样线程正在进行的 `mpv_get_property` 并发执行，直接崩溃。现在销毁排在采样串行队列之后执行，并同步作废在途采样链。
- **关闭「自动切换线路」后死流永久转圈**
  - 设置里关闭自动换线时，起播超时任务被一并取消——死流将没有任何出口复位「正在加载」徽标。起播超时本应与该开关解耦（超时只复位状态，是否换线由开关决定），现已修正。
- **StorageService 主线程卡顿**
  - 频道缓存的全量 JSON 编码（可达数 MB）此前在 barrier 块内进行，会卡住主线程所有同步读（黑名单/隐藏线路查询在播放热路径上）。编码移到后台串行队列，barrier 只做内存赋值与文件写入。
- **NetworkService 首次并发请求的数据竞争**
  - `lazy var session` 在镜像竞速的 TaskGroup 下会被并发首次初始化（Swift lazy 非线程安全）。改为构造期一次性创建。
- **静默刷新不再覆盖用户暂停**
  - 回前台触发的静默线路刷新，在用户主动暂停期间不再强制重新起播（此前会复位暂停状态、声音突然响起、恢复按钮消失）。
- **播放失败后的状态清理**
  - 起播最终失败后引擎仍报「播放中」：单击会被当成"暂停一个从未播出的流"、锁屏 NowPlaying 残留播放状态。失败时统一复位 `isPlaying`、取消 30s 无声无画看门狗。
- **协议白名单三处统一为 http/https**
  - 解析器、起播循环、自动换线的白名单此前互相矛盾：单 rtmp 线频道进列表后必然「协议不支持」被自动跳台，白白消耗一轮切换周期。现在解析阶段即丢弃不支持的协议，三处口径一致。
- **启动时多发一次假「网络恢复」**
  - 网络监视器的首次路径更新此前被当作「无网络 → 恢复」边沿，导致每次启动多拉一轮全量源。首次更新现在只建立基线。
- **其它**
  - 频道名全部命中噪音词（如「高清直播」）时归一化 key 为空，多个不同频道被错误合并进同一条目；现回退用显示名作 key。
  - DNS 解析失败不再缓存为 2 分钟硬失败：本地网络权限弹窗/captive portal 期间不会误杀正常线路。
  - `stop()` 现在释放旧 AVPlayerItem 及其解码缓冲；音量控件随 keyWindow 重建重新挂载，音量手势不再静默失效。
  - Info.plist 拼写修正：`UIInterfaceStyle` → `UIUserInterfaceStyle`（暗色模式对系统组件生效）。

## 说明

- 仅支持 iOS 16 及以上的 ARM64 设备；播放内核与依赖（libmpv / MPVKit）与 v2.4.10 一致。
