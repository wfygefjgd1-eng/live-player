# TV go iOS 2.0

- 全面升级为 VLCKit（VLC/FFmpeg）主播放内核，显著扩展直播格式兼容性。
- 针对 1080i 隔行 HLS 使用 VLC 去隔行播放，解决部分来源有声音但画面像幻灯片的问题。
- 保留 AVPlayer 自动兜底；VLC 无法起播时会自动切换，无需手动操作。
- 实时诊断页面新增当前播放内核，并支持 VLC 帧率、丢帧、分辨率和状态监测。
- 每次进入或重新激活 App 时，都会重新加载当前选中的来源。
- 安装后的 App 名称改为“TV go”，移除“正式版”字样。

仅支持 iOS 16 及以上的 ARM64 设备。
