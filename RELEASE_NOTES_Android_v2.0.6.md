# TV go Android ARM64 2.0.6

本轮修复聚焦自动换线/手动换线的核心交互、线程与内存问题，以及若干误拉黑正常线路的边缘场景。

## 修复

- **手动换线被偏好线路覆盖（关键）**
  - 根因：换线手势/遥控左右键设置的目标线路索引，会被「本台首次尝试时优先使用历史偏好线路或首条未拉黑线路」的逻辑覆盖。只要该台存在偏好线路（播放成功过一次就会有），左右键换线播回来的仍是原来的线路。
  - 修复：显式换线（手势/遥控）时跳过偏好覆盖，用户选哪条播哪条；换台后仍按信誉排序自动选线。
- **快速融合模式语义修正**
  - 「快速」模式此前实际并发拉取全部 10+ 个源（展开镜像后可达数十请求、瞬时内存峰值高），且频道列表来自"最先解析成功的任意源"。现改为与界面文案一致的「顺序取首个可用源」（单源内部仍做镜像竞速）。
- **rtsp 线路误拉黑 24 小时**
  - rtsp 源此前被强制按 HLS 解析，起播必然失败并被写入 24h 黑名单。现按 ExoPlayer 内置 RTSP 支持正确构造播放项。
- **两处数据竞争**
  - 智能模式首批频道对象在后台线程被继续修改、主线程同时遍历，重叠时会触发 ConcurrentModificationException 崩溃。现在回帖主线程前做实例隔离拷贝。
  - 线路信誉表跨线程读写补齐互斥（黑名单判断、评分、偏好查询），并改为"锁内快照、锁外序列化"，避免保存时阻塞主线程。
- **saver 线程泄漏**
  - 线路信誉存储的后台保存线程从不关闭，每次 Activity 重建泄漏一个常驻线程。现在退出时停掉线程并冲刷未落盘的数据。
- **其它**
  - 播放错误回调补齐代次校验：换线瞬间到达的旧错误不再把从未尝试过的新线路拉黑 24h。
  - 缓存加载路径补上线路过滤规则，含非法 URL 的旧缓存不再触发"未尝试线路被误标失败"。
  - 退到后台时后台加载完成不再自动起播（避免画面未出、音频在后台播放）；回前台自动补起播。
  - 遥控空格键暂停现在与点按一致记录用户暂停意图，Home 往返后不会被强制续播。
  - 高位端口（≥10000）的合法线路不再被质量过滤误杀；列表刷新后选中高亮同步刷新。
  - 深色模式切换不再重建 Activity（configChanges 增加 uiMode）。

## 说明

- 最低支持 Android 5.0，本次发布包仅面向 ARM64 设备。
