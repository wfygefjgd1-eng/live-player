# TV go Android ARM64 2.0.1

- **切台「正在加载」提示不再提前消失**：此前切台后频道 OSD（频道号/名称）固定 2.5 秒自动隐藏，早于声画真正出来。现改为：切台时 OSD 保持可见，直到 ExoPlayer 确认出画（`STATE_READY` + 正在播放）后才开始隐藏倒计时，与 iOS 行为对齐。
- 保留现有 ExoPlayer 播放、频道管理、手势、遥控器操作和自动换线能力。
- 本次 GitHub 发布包仅面向 ARM64 Android 手机、电视和盒子。
